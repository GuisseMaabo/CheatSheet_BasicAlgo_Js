---
# Cheatsheet Basic algorithms using javascript
---
# This is a cheat sheet  of some Basic Javascript algorithms 
### _1) Multiplication, division, and comparison operators_
> Of Course there are other solutions to the problems below 

      Steps : 
        1)Write a function that takes two numbers, say a and b, as arguments
        2)If a is smaller than b, divide a by b
        3)Otherwise, multiply both numbers
        4)Return the resulting value
```
 function myFunction(a,b){
     return a < b ? a / B : a * b ; 
 }
```
### _2) Check if a number is even_


      Steps : 
        1) Write a function that takes a number as argument
        2) If the number is even, return true
        3) Otherwise, return false  
```
 function myFunction(a){
     return a % 2 == 0 ? true : false ; 
 }
```
### _3) Check if a number is a whole number_


      Steps : 
        1) Write a function that takes a number as argument
        2) If the number is a whole number (has no decimal place), return true
        3) Otherwise, return false
```
 function myFunction(a){
     return a % 1 == 0 ? true : false ; 
 }
```
### _4) Check whether a string contains another string and concatenate_


      Steps : 
        1) Write a function that takes two strings, say a and b, as arguments
        2) If a contains b, append b to the beginning of a
        3) If not, append it to the end
        4) Return the concatenation
```
 function myFunction(a,b){
     return a.indexOf(b) === -1 ? a + b : b + a ; 
 }
```

### _5) Square Every Digit

      Steps : 
        1) Square every digit of a gven number and concatenate them
        2) For example, if we run 9119 through the function, 
            811181 will come out because 9² is 81 and 1² is 1
        3) The function accepts an integer an return an integer
```
 function squareDigits(num){
     return Number(
     num
     .toString()
     .split('')
     .map(num=>num**2)
     .join('')
     );
 }
```

### _6) Exes and Ohs

      Steps : 
        1) Check to see if a string has the same amount of 'x's and 'o's
        2) The method must return a boolean and be case insensitive
        3) The string can contain any char. 
Examples: input/output
* XO("ooxx") => true
* XO("xooxx") => false
* XO("ooxXm") => true
* XO("zpzpzpz") => true          // when no "x" and "o" is present return true
* XO("zzoo") => false
```
 function XO(str){
    let test = Array.from(str.toLowerCase());
    return test.filter(x=> x ==="x").lenght == test.filter(x=> x ==="o").length;
 }
```